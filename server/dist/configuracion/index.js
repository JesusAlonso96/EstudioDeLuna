"use strict";
/*import { environment } from '../global/environment';
import { environmentprod } from '../global/environment.prod';

if (process.env.NODE_ENV == 'production') {
    module.exports = environmentprod;
} else {
    module.exports = environment;
}*/ 
